# Rebel Contact Us Form

## Getting started

- `npm install` or `yarn install` to install all dependencies
- `npm start` or `yarn start` to run the local development environment
- `npm test` or `yarn test` to run the tests
